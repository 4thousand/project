import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddcarparkPage } from './addcarpark';

@NgModule({
  declarations: [
    AddcarparkPage,
  ],
  imports: [
    IonicPageModule.forChild(AddcarparkPage),
  ],
})
export class AddcarparkPageModule {}
